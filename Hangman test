#Hang man
import random

#Need to do the letters and stuff

pack1 = ['doubt', 'envy', 'greed', 'fury', 'sloth',
  'depression', 'disgust', 'fear', 'love', 'pity']
pack2 = ['fly', 'ox', 'cougar', 'zebra', 'aardvark',
  'triceratops', 'crocodile', 'hippopotamus', 'dingo', 'spider']
pack3 = ['revolutionary', 'physics', 'explicit', 'branch', 'inflation', 'radical', 'critical', 'monarch', 'harmony', 'sandwich']

correctLetters = list()

gameStartUp = True

def hangStart():
  correctLetters = list("_" * len(word))
  gameStartUp = False
  return correctLetters,gameStartUp

def hangGame(correctLetters,lives):
  print ("=" * 25)
  print (correctLetters)
  print (word)
  guess = str.lower(input("Enter guess: "))
  return (checkRight(splitWord,guess,lives))

def checkRight(splitWord,guess,lives):
  #print ("hi")
  if guess in splitWord:
    for index,letter in enumerate(splitWord):
      #print ("Index == ", index)
      #print ("Letter ==", letter)
      if letter == guess:      
        correctLetters[index] = guess
        splitWord[index] = "_"
      else:
        pass
      #print (correctLetters)
      #print (splitWord)
    return (correctLetters,splitWord,lives)
  else:
    lives -= 1 #get this to do it right
    print (lives)
    print ("wrong")
    return lives    

def wordSplit():
  s = word
  splitWord = list(s)
  return splitWord
  
lives = 9

packChoose = True
while packChoose == True:
  packChoice = input("What pack do you want to play? (1|2|3)")
  if packChoice == "1":
    hangmanOption1 = True
    word = random.choice(pack1)
    splitWord = wordSplit()
    print (splitWord)
    break
  elif packChoice == "2":
    hangmanOption2 = True
    word = random.choice(pack2)
    splitWord = wordSplit()
    print (splitWord)
    break
  elif packChoice == "3":
    hangmanOption3 = True
    word = random.choice(pack3)
    splitWord = wordSplit()
    print (splitWord)
    break
  else:
    print ("Invalid answer")
    
done = False

while not done:    
  if hangmanOption1 == True:
    if gameStartUp == True:
      correctLetters,gameStartUp = hangStart()
      #print (gameStartUp)
    else:
      pass
    correctLetters, splitWord, lives = hangGame(correctLetters,lives)
    print("correctLetters == ", correctLetters)
    print("splitWord == ", splitWord)
    print (lives)
